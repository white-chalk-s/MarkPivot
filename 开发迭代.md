# MarkPivot 开发迭代

> 版本：Roadmap v2.0  
> 日期：2026-09-23  
> 目标：从“文档互转工作台”收敛为“本地文档规范化工具”  
> 原则：不推倒重来，复用已有解析、导出、样式提取、批量与报告能力。

---

## 1. 当前基础

现有 MarkPivot 已经具备：

- 本地优先的 HTML 工作台
- Word / Markdown / Excel 处理能力
- Markdown 语义中枢
- Word 导入 / 导出
- Word 样式提取
- Excel 视觉元数据处理
- 批量转换
- 转换报告
- File System Access API 文件关联
- WPS 兼容专项处理
- Playwright QA

这些能力作为新方向的工程基础保留。

---

## 2. 转向原则

### 2.1 不重写底层

优先复用：

- DOCX 解析
- DOCX 生成
- 样式提取
- 文件资源池
- 批量处理
- 报告机制

### 2.2 重构产品中心

从：

```text
格式互转
```

转成：

```text
上传文件
→ 选择规范
→ 自动识别 / 微调
→ 批量导出
```

### 2.3 单文件与批量共用一套实现

不要维护：

- 单文件模式
- 批量模式

两套逻辑。

统一：

```text
Task.files.length === 1
```

或：

```text
Task.files.length > 1
```

只影响展示和导出方式。

---

## 3. 开发目标架构

建议逐步重构为：

```text
src/
├─ core/
│  ├─ document-model.js
│  ├─ specification.js
│  ├─ task-model.js
│  └─ file-override.js
│
├─ parsers/
│  ├─ docx-parser.js
│  ├─ markdown-parser.js
│  └─ xlsx-parser.js
│
├─ analyzers/
│  ├─ semantic-mapper.js
│  └─ spec-extractor.js
│
├─ rules/
│  └─ rule-engine.js
│
├─ renderers/
│  └─ docx-renderer.js
│
├─ ui/
│  ├─ home/
│  ├─ spec-select/
│  ├─ workspace/
│  ├─ spec-library/
│  └─ export/
│
└─ styles/
   ├─ tokens.css
   ├─ components.css
   └─ pages.css
```

开发源码模块化。

最终仍可构建为：

> 单 HTML / 本地离线发行包。

---

# 4. Milestone 0：修复现有基线

目标：

> 恢复一个可信、可重复运行的开发基线。

### 工作

- [ ] 统一入口文件命名
- [ ] 修复 QA 中旧 HTML 文件名引用
- [ ] README / AGENTS / docs / verify.mjs 同步
- [ ] 重新跑全量 QA
- [ ] 清理 ExcelJS / SheetJS 文档矛盾
- [ ] 清理 PapaParse 不存在的技术栈描述
- [ ] 重新记录当前真实 PASS 数

### 验收

- `npm run qa:all` 可重复执行
- 入口不存在 ENOENT
- 文档与代码一致
- 当前测试结果可复现

---

# 5. Milestone 1：建立 UI Foundation

目标：

> 在继续开发业务页面之前固定最小设计系统。

### 工作

- [ ] 建立 `tokens.css`
- [ ] 统一字体
- [ ] 统一品牌橙 / 暖白 / 中性灰
- [ ] Button
- [ ] Input
- [ ] Select
- [ ] Tabs
- [ ] Stepper
- [ ] Card
- [ ] Upload Dropzone
- [ ] File Item
- [ ] Status
- [ ] Form Section

### 代表页面

先实现：

1. 首页上传
2. 选择规范
3. 自动识别 / 微调工作台

### 验收

- 三个页面完全复用基础组件
- 页面中不出现大量私有颜色
- 不出现重复按钮 CSS
- 不出现随机 spacing
- 视觉达到可开发使用水平，不追求最终 polish

---

# 6. Milestone 2：Task 模型

目标：

> 产品正式从“页面状态”升级为“任务状态”。

定义：

```text
Task
├─ id
├─ files[]
├─ specification
├─ fileOverrides
├─ currentFileId
└─ output
```

### 工作

- [x] 新建 Task
- [x] 返回首页结束 / 离开 Task
- [x] 上传 1 个或多个文件
- [x] 当前文件状态
- [x] 任务级规范
- [x] 文件级 override
- [x] Task 不依赖一级工作台页面

### 验收

- 每次首页创建的是独立 Task
- 新任务不继承旧任务文件
- 规范库数据与 Task 数据分离
- Task 内修改不会自动污染规范库

---

# 7. Milestone 3：Specification Model

目标：

> 建立全项目统一规范模型。

### 第一版支持

```text
Specification
├─ Page
├─ Body
├─ Heading1
├─ Heading2
├─ Heading3
├─ Table
├─ Image
├─ Header
└─ Footer
```

### Page

- 纸张
- 方向
- 上下左右边距

### Body

- 字体
- 字号
- 行距
- 首行缩进
- 段前段后

### Heading

- 字体
- 字号
- 粗体
- 对齐
- 段前段后
- 分页规则

### Table

- 字体
- 字号
- 对齐
- 边框
- 跨页

### Image

- 最大宽度
- 对齐
- 比例

### 验收

- UI 不直接读写散落 CSS
- 所有设置经过 Specification
- 修改规范不会修改原文语义
- Specification 可以 JSON 序列化

状态：2026-09-23 已完成，QA `49 PASS / 0 FAIL / 0 SKIP`。

---

# 8. Milestone 4：Spec Extractor

目标：

> 上传一个正确 DOCX，可以自动生成 Specification 初稿。

复用现有能力：

- `styles.xml`
- `theme1.xml`
- `document.xml`
- `basedOn`
- themeColor
- themeShade / Tint

新增：

- [x] 页面尺寸
- [x] 页边距
- [x] Section
- [x] Heading 1–3
- [x] Normal / Body
- [x] 表格基础格式
- [x] 图片约束
- [x] 页眉页脚基础信息

### 交互

模板上传后展示：

```text
已提取：

页面
正文
一级标题
二级标题
三级标题
表格
图片
```

用户可进入工作台继续修改。

### 验收

- 同一模板重复提取结果稳定
- 提取失败字段明确标记
- 不猜测不存在的格式
- 提取结果能直接驱动工作台

---

# 9. Milestone 5：Semantic Mapper v1

目标：

> 识别原始 Word 中“这段内容是什么”。

第一版支持：

- Heading 1
- Heading 2
- Heading 3
- Body
- Ordered List
- Unordered List
- Table
- Image
- Figure Caption
- Table Caption

识别依据：

- Word Style
- 字号
- 字重
- 对齐
- 段落长度
- 编号模式
- 上下文

输出：

```text
semanticType
confidence
evidence
```

例如：

```json
{
  "semanticType": "heading1",
  "confidence": 0.91,
  "evidence": [
    "style=Heading 1",
    "short paragraph",
    "chapter numbering"
  ]
}
```

### 原则

不要一开始依赖大模型作为唯一判断。

优先：

```text
确定性规则
+
启发式评分
+
用户可修改
```

### 验收

- [x] 已有 Heading Style 的文档优先正确
- [x] 手工格式标题可以识别
- [x] 低置信度不静默强改
- [x] 用户可在工作台看到异常文件

状态：2026-09-23 已完成，QA `53 PASS / 0 FAIL / 0 SKIP`。

---

# 10. Milestone 6：自动识别 / 微调工作台

目标：

> 建立产品核心页面。

固定三栏：

```text
文件列表 | 实时文档预览 | 规范设置
```

### 左栏

- 文件列表
- 当前文件
- 状态

### 中栏

- 原文
- 当前效果
- Word 纸面
- 页码
- 缩放

### 右栏

- 当前文件 / 全部文件
- 当前模板
- 上传新模板
- 页面
- 正文
- 标题
- 表格
- 图片

### 核心规则

修改 Specification：

```text
→ 实时更新预览
```

作用范围：

```text
当前文件
```

写入：

```text
File Override
```

作用范围：

```text
全部文件
```

写入：

```text
Task Specification
```

### 验收

- [x] 修改正文设置后预览立即变化
- [x] 切换文件时预览正确
- [x] 全部文件与当前文件作用域不会混淆
- [x] 可恢复模板值
- [x] 上传新模板后重新生成规范初稿

状态：2026-09-23 已完成，QA `55 PASS / 0 FAIL / 0 SKIP`。

---

# 11. Milestone 7：Rule Engine

目标：

> 将 Document Model 按 Specification 映射为目标文档。

形式：

```text
Document Semantic Node
+
Specification
+
File Override
↓
Rendered Style
```

优先覆盖：

- Page
- Body
- Heading 1–3
- Table
- Image

### 原则

格式变化不得污染内容语义。

继续保持：

> 内容与呈现分离。

### 工作

- [x] 将 Semantic Node + Specification + File Override 编译为 Rendered Style
- [x] 覆盖 Page / Body / Heading 1–3 / Table / Image
- [x] 当前效果按规则涂绘；原文模式清除涂绘并可随时重渲染
- [x] 导出套用规则（启发式标题在导出 holder 提升为 H1–H3），不改 Markdown
- [x] `?qa=` 跳过引擎，保持 V4.1 标签导出

### 验收

- [x] 同一规范作用于多个文件结果一致
- [x] 当前文件 override 优先于任务规范
- [x] 切换规范不修改原文内容
- [x] 原文可随时重新渲染

---

# 12. Milestone 8：批量导出

目标：

> 单文件与多文件使用同一套导出逻辑。

### 单文件

```text
DOCX
```

### 多文件

```text
ZIP
├─ 01 xxx.docx
├─ 02 xxx.docx
├─ 03 xxx.docx
└─ specification.json
```

### 工作

- [x] 导出进度
- [x] 单文件失败不阻塞全部任务
- [x] 批量报告
- [x] 文件名保持
- [x] 文件冲突处理
- [x] 取消

### 验收

- [x] 20 个测试文件可连续输出
- [x] 一个文件失败时其他文件仍可导出
- [x] ZIP 文件结构稳定
- [x] Word / WPS 可打开

---

# 13. Milestone 9：性能与大文件

目标：

> 从“能用”升级到“敢喂真实项目文件”。

### 工作

- [x] Worker
- [x] DOCX 解析异步化
- [x] 分片处理
- [x] 批量进度
- [x] 取消任务
- [x] 内存释放
- [x] 大图片优化
- [x] 任务级错误隔离

### 测试档位

至少覆盖：

- 5 MB
- 20 MB
- 50 MB
- 100+ 页
- 500+ 页
- 50+ 图片
- 20+ 表格

实施说明：5 / 20 / 50 MB 与 100+ / 500+ 页以 `jobPlan()` 档位契约验收，每次 `qa:all` 不生成 50 MB 二进制。100+ 段、20 表、50 图由 `tools/fixtures/large-doc.docx` 覆盖。

### 验收

- [x] UI 不长时间冻结
- [x] 可见进度
- [x] 可以取消
- [x] 一个文件异常不导致整个任务崩溃

---

# 14. Milestone 10：真正离线交付

当前目标：

> 首次使用也不依赖 CDN。

发行形式优先：

```text
MarkPivot/
├─ MarkPivot.html
├─ vendor/
└─ templates/
```

或构建时将依赖完整内联。

### 工作

- [x] 运行库本地化
- [x] 删除首次联网要求
- [x] 离线启动验证
- [x] 内网环境验证
- [x] Windows + Edge / Chrome 验证

暂不急于：

- Electron
- Tauri
- 安装包

除非浏览器权限成为业务瓶颈。

---

# 15. Milestone 11：规范库

目标：

> 让 Specification 成为可复用资产。

支持：

- [x] 新建
- [x] 重命名
- [x] 复制
- [x] 编辑
- [x] 删除
- [x] 导入 JSON
- [x] 导出 JSON
- [x] 从 Word 创建

后续再增加：

- 版本
- 标签
- 搜索
- 最近使用

### 原则

Task 内微调：

> 不自动覆盖规范库。

用户明确点击：

> 保存为新规范

才写入规范库。

---

# 16. QA 策略

现有 QA 思路保留，并新增规范化专项。

### 必测

#### 语义不变

切换规范前后：

```text
Document semantic diff = 0
```

#### Specification

- JSON roundtrip
- 默认值
- override 优先级
- 缺失字段

#### 批量

- 多文件
- 部分失败
- 取消
- 重试

#### DOCX

- Word 打开
- WPS 打开
- OOXML 核心结构
- 页面
- 标题
- 表格
- 图片

#### UI

截图基线优先覆盖：

1. 首页
2. 规范选择页
3. 工作台

---

# 17. 迭代优先级

建议顺序：

```text
P0 现有基线修复
↓
P0 UI Foundation
↓
P0 Task Model
↓
P0 Specification Model
↓
P1 Spec Extractor
↓
P1 Semantic Mapper
↓
P1 工作台
↓
P1 Rule Engine
↓
P1 批量导出
↓
P2 大文件性能
↓
P2 真离线
↓
P2 规范库增强
```

不优先投入：

- PPT
- PDF 编辑
- 云端协作
- 账号
- AI 写作
- Excel⇄Markdown 功能扩张
- 深色模式
- 大量视觉特效

---

# 18. 开发过程中 UI 与功能如何并行

不要：

> 功能全部做完后再统一美化。

正确顺序：

```text
低保真确认结构
↓
UI Foundation
↓
三个代表页高保真
↓
组件实现
↓
业务开发
↓
规范边做边补
↓
功能稳定
↓
最后视觉 Polish
```

新增页面优先套现有页面模板。

如果发现某个基础组件不合适：

> 修改全局系统，不给单页打补丁。

---

# 19. Definition of Done

每一个功能完成至少满足：

### 功能

- 主链路可用
- 错误有提示
- 状态可恢复

### UI

- 使用设计 Token
- 使用公共组件
- 无明显单页私有风格

### 数据

- 不污染原文语义
- Task 与 Spec 边界明确

### 兼容

- Word 可打开
- WPS 可打开

### 测试

- 相关自动测试通过
- 原有关键测试无回归

---

# 20. 最终目标

MarkPivot 的开发方向不再围绕：

> 再增加一种文件转换。

而围绕四件事：

```text
1. 更准确地理解文档
2. 更稳定地理解规范
3. 更一致地执行规范
4. 更高效地批量交付
```

任何新增需求，进入排期前必须回答：

> 它是否明显提升了以上四项中的至少一项？

如果没有，默认不进入当前主线。
