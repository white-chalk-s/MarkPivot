# MarkPivot · 文档互转工作台 · 更新日志 + 复杂度账本

> 每批一条，格式固定：做了什么 / 验证（命令+数字）/ 账本（数字对比）/ 有意变更 / 遗留。不带数字的条目是日记，不是日志。
> 说明：历史批次的“遗留”保留当日状态；当前状态以本日志最新条目和 [交接表](交接表.md) 为准。

## 2026-09-03 · 文档治理收尾（稳定基线入口）

- 做了什么：把项目资料按受众和生命周期重新分层；新增根目录 `README.md`、`AGENTS.md`、`docs/使用说明.md`、`docs/文档导航.md` 和 `docs/archive/README.md`；将定位、总则、设计、开发、日志与交接资料统一到 `docs/`；将立项方案和两个历史原型移动到 `docs/archive/`。
- 验证：逐项核对当前 HTML 入口、`package.json`、现役 Markdown、归档路径和文档链接；产品自动化基线仍为 `npm.cmd run qa:all` 的 `45 PASS / 0 FAIL / 0 SKIP`，本次未修改产品运行代码。
- 账本：产品 HTML `301382 B` 不变；现役文档入口由根目录分散文件收敛为 `README.md` + `docs/`；历史材料保留原文件内容，仅改变归档位置。
- 有意变更：README 面向所有使用者，使用说明面向实际操作，开发文档/交接表面向维护者，项目总则/AGENTS.md 面向开发与 Agent；归档内容不再作为当前需求依据。
- 遗留：M3.4 大文件分片、Worker、进度与取消仍排队；WPS 前导空隙已由用户确认属于渲染差异，跨版本像素级细节仍需目标环境复核；旧原型文件已归档，根目录空目录已清理。

## 2026-09-03 · V4.1（9 列中文表格 WPS 排版修正）

- 做了什么：针对用户截图中的 9 列主数据识别表，补充 8 列以上宽表紧凑预览规则（`10px` 字号、`4px` 内边距）；DOCX 宽表显式写入 `w:tblCellMar` / `w:tcMar=60 twips`，表格单元格段后距归零，保持 `fixed`、`gridCol`、`tcW`，并确认不写入 `textDirection`。
- 验证：`npm.cmd run qa:all`；`45 PASS / 0 FAIL / 0 SKIP`，原有 42 项全部保持通过；V4.1 9 列回归中间 6 个五字表头均为 `1` 行，浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`；宽表 DOCX `36041 B`，截图 `v4-wide-table-after-fix.png=6634 B`。
- 账本：产品 HTML `300365 B` → `301382 B`（`+1017 B / +1.0 KB / +0.3%`）；新增 `tools/fixtures/v4-wide-table.md=493 B`；QA 报告 `tools/output/v4-report.json=12777 B`；新增宽表 DOCX `36041 B`。
- 有意变更：8 列以上表格为保证中文可读性使用紧凑字号和内边距；只影响宽表预览和 Word 导出，不改变 Markdown 语义，也不改变 7 列以内表格的原有布局比例。
- 遗留：当前环境未安装 WPS，人工截图复核仍待用户确认；下一批仍为 M3.4 大文件分片、Worker、进度与取消，历史 Token 字面值清扫顺延。

## 2026-09-03 · V4（表格布局与缩进修正）

- 做了什么：新增共享表格列宽算法，按全角 `1`、半角 `0.5` 计算最大内容宽度，加入 4 字符最小列宽和单元格内边距预算；Markdown/Word 预览注入 `<colgroup>` 与 `table-layout: fixed`，DOCX 写入 `gridCol`、每格 `tcW` 和 fixed 布局。正文首行缩进改为 marker 后处理单写 `w:firstLineChars="200"`，删除 `w:firstLine` 双写；列表编号 level0–3 校准为 `420/420`、`840/420`、`1260/420`、`1680/420`。
- 验证：`npm.cmd run qa:all`；`44 PASS / 0 FAIL / 0 SKIP`，原有 42 项全部保持通过；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`。V4 预览列宽 `27.2727/15.1515/57.5758%`，DOCX `gridCol=2462/1368/5196 twips`，比例偏差 `<0.2` 个百分点；正文 `firstLineChars=200`、`firstLine=0`；列表 `left/hanging=420/420`；长中文列不再逐字竖排。
- 账本：产品 HTML `292666 B / 285.8 KB` → `300365 B / 293.3 KB`（`+7699 B / +7.5 KB / +2.6%`）；新增 `tools/fixtures/v4-table.md=399 B`；V4 DOCX `27868 B`；QA 报告 `tools/output/v4-report.json=12184 B`；V4 桌面/移动截图 `25181 B / 25797 B` 写入 `tools/output/playwright/v4/`。
- 有意变更：预览与导出使用同一列宽比例；正文 DOCX 不再同时写 `firstLine` 与 `firstLineChars`；V4 使用独立报告与产物目录；未改动合并单元格语义、Excel 网格列宽和跨页样式。
- 遗留：WPS 人工打开 V4 DOCX 并截图复核待用户确认；下一批为 M3.4 大文件分片、Worker、进度与取消，历史 Token 字面值清扫仍顺延。

## 2026-09-03 · F3（封面页格式）

- 做了什么：将正文开头的 `::title` 与紧随或间隔一个空行的 `::desc` 识别为封面区；仅 `::title` 也可形成封面，中部 `::title` 仍是普通标题。Markdown/Word 预览新增编辑型居中封面、顶部强调线、无边框底纹副标题和封面后分页分隔；Word 导出写入居中、`spacing before=1800` 和 `PageBreak`，封面区豁免首行缩进。
- 验证：`npm.cmd run qa:all`；`42 PASS / 0 FAIL / 0 SKIP`，原有 39 项全部保持通过；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`。带说明封面左右预览 `1/1`、自动分页 `1/1`；仅 title 和中部 title 边界通过；DOCX `jc=center`、`before=1800`、`pageBreak=1`、封面 `firstLine=0`、正文 `firstLineChars=200`。
- 账本：产品 HTML `284080 B / 277.4 KB` → `292666 B / 285.8 KB`（`+8586 B / +8.4 KB / +3.0%`）；新增 `tools/fixtures/cover.md=353 B`；封面 DOCX `25117 B`；QA 报告 `tools/output/f3-report.json`；桌面/移动封面截图写入 `tools/output/playwright/f3/`。
- 有意变更：自动分页是结果视图派生节点，Word 预览回写时不污染 Markdown；F3 独立使用 `f3` 报告和产物目录；封面后正文继续沿用默认 2 字符首行缩进。
- 遗留：下一批为 M3.4 大文件分片、Worker、进度与取消；封面日期/作者语法、背景图、多封面和 Excel/CSV 封面链路明确不做。

## 2026-09-03 · V2（Word 模板提取准确性与样式控件升级）

- 做了什么：新增参考 Word 模板提取夹具，修正 OOXML 无 `w:color` 时的默认黑语义，支持 `themeColor` 与 `themeShade` / `themeTint` 换算；样式抽屉新增字段来源徽标、字体/字号/颜色下拉或色板、表头底色一键清除和自定义输入；样式 JSON 升级为 v2 并保留 `sources`。Markdown `*...*`、Markdown/Word 预览和 DOCX 导出统一为非倾斜中文着重号。
- 验证：`npm.cmd run qa:all`；`39 PASS / 0 FAIL / 0 SKIP`，原有 36 项全部保持通过；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`。正文 `#000000`、Title `#1b2e4e`、H1 `#f1975a`、H2 `#000000`、表头 `#698ed0` 通过；来源徽标 `48`、字体/字号/颜色控件 `1/17/10`、表头清除 `1`；DOCX `w:em=1`、`w:i`/`italics=0`。
- 账本：产品 HTML `266587 B / 260.3 KB` → `284080 B / 277.4 KB`（`+17493 B / +17.1 KB / +6.6%`）；新增 `tools/fixtures/reference-template.docx=7725 B`；QA 报告 `tools/output/v2-report.json=10066 B`；V2 样式抽屉和移动端截图写入 `tools/output/playwright/v2/`。
- 有意变更：正文默认色由历史预设深灰改为 `#000000`；样式 JSON 版本升级为 `2` 并增加字段来源；`em` 改写为 `w:em val="dot"` 着重号；V2 报告和截图使用独立 `v2` 产物目录。
- 遗留：F3 封面页格式是下一批；复杂条件表格样式、更深层 OOXML 版式、M3.4 大文件分片/Worker/进度取消和 OMML 公式继续顺延；历史兼容 CSS/常量仍有 `187` 个 hex 与 `15` 个 rgba 字面值待 Token 清扫。

## 2026-09-03 · F2（文档打开与刷新）

- 做了什么：导入组新增「打开」，在支持 File System Access API 的浏览器中选择 `.md/.txt` 并建立本次会话文件关联；Markdown 面板新增「刷新」「保存」和默认关闭的「自动同步」。刷新从磁盘重读，保存通过 `createWritable()` 写回当前 Markdown，编辑器支持 Ctrl+S；页脚展示读写/只读关联与文件名。
- 验证：`npm.cmd run qa:all`；`36 PASS / 0 FAIL / 0 SKIP`（原有 30 项全部保持通过，新增静态契约、打开/只读关联、刷新保存双向链路、刷新保存冲突、自动同步/Ctrl+S、无 API 降级 6 项）；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`。mock FileSystemFileHandle 已覆盖 `getFile()`、`lastModified`、`createWritable()` 和双向冲突确认。
- 账本：产品 HTML `250967 B / 245.1 KB` → `266587 B / 260.3 KB`（`+15620 B / +15.3 KB / +6.2%`）；fixture `sample.md=2309 B`、`sample.docx=9136 B`、`sample.xlsx=8772 B`；QA 报告 `tools/output/f2-report.json=9030 B`，桌面/移动视觉检查截图为 `tools/output/playwright/f2-desktop.png=124755 B` 与 `tools/output/playwright/f2-mobile.png=34888 B`。
- 有意变更：关联句柄只保存在内存，不进入本机草稿；无 API 时回退普通文件选择；自动同步默认关闭，本地编辑时暂停；F2 不关联 Word/Excel、目录、IndexedDB 或云端。
- 遗留：下一批为 V2 Word 模板提取准确性与样式控件升级，随后处理 F3 封面页格式；M3.4 大文件分片、Worker、进度/取消和 OMML 公式可逆化继续顺延；历史兼容 CSS/常量仍有 `187` 个 hex 与 `15` 个 rgba 字面值待 Token 清扫。

## 2026-09-03 · V1（WPS 导出兼容、滚动主权与正文缩进）

- 做了什么：桌面工作台改为 `height:100dvh` 锁高，Markdown/Word/Excel 结果区恢复面板内部滚动，`≤1080px` 释放为整页滚动；修复 `.excel-stage` 的 `display:flex` 覆盖 `hidden`、导致 Excel 信息条泄漏到 Word 模式的问题。样式抽屉新增默认开启的「正文首行缩进」开关（2 字符），开启时仅清理普通正文段落行首全/半角空白，预览与 Word 导出同步。DOCX 代码块改为 SimSun `ascii/hAnsi/eastAsia` 三通道，按最长行显示宽度（全角 `1`、半角 `0.5`）自适应字号并写入 `wordWrap=0`；行内 code 保持 Consolas。新增 WPS 字符画 fixture、滚动/模式互斥/缩进/OOXML 回归及移动端现场截图。
- 验证：`npm.cmd run qa:all`；`30 PASS / 0 FAIL / 0 SKIP`（既有 26 项全部保持通过，V1 新增 4 项）；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`。桌面 `page=1100/1100`，Markdown `9118>838`，Word `9263>884`，Excel grid-wrap `3468>798`，移动端 `10109>900`；DOCX `rFonts=SimSun` 三通道、最小字号 `8` half-points、`wordWrap=0`、正文 `firstLineChars=200`。
- 账本：产品 HTML `235928 B / 230.4 KB` → `250967 B / 245.1 KB`（`+15039 B / +14.7 KB / +6.4%`）；fixture `sample.md=2309 B`、`sample.docx=9130 B`、`sample.xlsx=8771 B`；QA 报告 `tools/output/v1-report.json=7885 B`，V1 兼容性 DOCX `27802 B`，移动端截图 `45739 B`。
- 有意变更：新增样式持久化键 `--body-first-line-indent`，默认值为 `2em`；DOCX 代码块默认不折行并允许字号降至 `8` half-points；QA 产物独立写入 `tools/output/playwright/v1/`，保留历史 U2/U3/F1 报告与产物。
- 遗留：F2 文档打开/刷新/保存与冲突保护是下一批；V2 Word 模板提取准确性随后处理；M3.4 大文件分片、Worker、进度/取消和 OMML 公式可逆化仍待后续；历史兼容 CSS/常量仍有 `187` 个 hex 与 `15` 个 rgba 字面值待 Token 清扫。

## 2026-09-03 · U2/U3/F1（布局、预览与批量流水线）

- 做了什么：完成 U2 顶栏六段单行布局、导入/导出/工具分组、唯一 Word 主按钮、Excel 三层信息条并层和响应式折行；完成 U3 的 Office 风格 Word 默认预设（蓝系标题、蓝色链接、全网格表格、中文 Office 字体栈、段落底纹）与 Obsidian 风格 Markdown 预览；完成 F1 纯旁路批量转换抽屉，支持 `.docx/.xlsx/.xls/.csv/.md` 多选、MD/Word/Excel 三目标、逐文件状态、P0 确认、重名序号、JSZip 报告和编辑区零污染。另修复本地图片在沙箱释放前后的无效请求。
- 验证：`npm.cmd run qa:all`；26 项 PASS、0 项 FAIL、0 项 SKIP；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`；批量目标覆盖 MD `3` 文件、Word `2` 文件、Excel `2` 文件，另以 CSV `1` 文件验证 Excel 中枢入 MD；MD/Word/Excel 产物分别通过 ZIP、DOCX 结构和 ExcelJS 回读；编辑区 MD diff 均为 `0`；1600px 顶栏单行、600px 窄屏折行已做 Playwright 视觉检查。
- 账本：产品 HTML `191421 B / 186.9 KB` → `235928 B / 230.4 KB`（+44507 B / +43.5 KB）；`:root` Token `42` → `81`；Google Fonts 外链 `3` → `0`；本批 QA 报告写入 `tools/output/u2-u3-f1-report.json`（`6826 B`），批量产物写入 `tools/output/playwright/u2-u3-f1/`。
- 有意变更：默认 UI 切换为浅灰蓝画布 + 蓝紫 accent；Word 默认“清爽文档”切换为 Office 蓝系并将样式持久化键升级为 `document-workbench-style-v2`；批量任务使用隔离 state，不写入本机草稿或当前编辑区；新增 `tools/output/u2-u3-f1/`，保留 M3.3 历史报告与产物。
- 遗留：历史兼容 CSS/导出常量仍有白名单外字面色值 `187` 次、`rgba` `15` 次，当前不再驱动新默认视觉；完整 Token 清扫留作后续治理项。M3.4 大文件分片、Worker、进度/取消和 OMML 公式可逆化仍待后续。

## 2026-09-03 · 规划变更（U2 拆分 + U3/F1 立项）

- 做了什么：用户实操反馈三问题（①缺批量导入/导出 ②Word 视图像 MD 不像 Word ③按钮排版乱），现场诊断全部坐实；路线拍板：U2 拆出「MD 预览校准」、新增 U3（预览真实感·Word 视图 Office 化）与 F1（批量转换流水线·纯旁路），顺序 **U2 → U3 → F1**，M3.4 顺延至 F1 之后；spec 草案落交接表 2.8 节
- 验证：无业务代码。诊断依据：grep「批量/batch/multiple」命中=0；工具栏现状截图 + `.word-paper` 样式审读定位「像 MD」根因（标题负字距 / 链接绿 #486f25 / md 风格组件 / 细线表格 / 全无衬线）
- 账本：无变化（HTML 191421 B / 186.9 KB 不变）；路线批数 5→7（+U3、+F1）
- 有意变更：无（纯规划批，无代码/默认值变化）
- 遗留：U1 仍为当前批待开工；U2/U3/F1 草案待各批开工时升格细化

## 2026-09-02 · M0-pre（立项）

- 做了什么：规划文档定稿（当时的 `方案_文档互转工作台_2026-09-02.md`，现已归档）；按当时的 project-standard 补齐骨架六件套：项目定位 / 项目总则（5 铁律）/ 设计规范（Token 表）/ 交接表（M0 spec）/ 本日志 / 全局 ai-dev-process.md 登记表加行
- 验证：无业务代码。7.2 核对清单 10 项由全 ❌ → 结构性就位（基线与验证脚本待 M0 批建立后回填数字）
- 账本：原型 HTML 体积 16.1KB（Excel-Md v1.1）/ 48.5KB（word转md v2.0）存量记录；工作台 0KB（未开工）
- 有意变更：无（立项批）
- 遗留：① `tools/verify.mjs` 与 fixtures 由 M0 批交付；② 交接表「验证基线」栏待 M0 回填；③ 两个原型 HTML 保留作参考、不进产品，现已移动到 `docs/archive/原型/`

## 2026-09-02 · M3.2（真实文件回归）

- 做了什么：新增开发测试依赖边界（`package.json` / `package-lock.json`）、真实文件夹具生成器 `tools/fixtures/build.mjs` 和 Playwright 回归脚本 `tools/verify.mjs`。夹具覆盖 Markdown 全元素、DOCX Title/H1-H6/列表/链接/脚注/图片/合并表格，以及 XLSX 三 Sheet/类型/公式/样式/合并/列宽/行高；同时修复 Excel 稀疏行高导入后导出错位。
- 验证：`npm.cmd run qa:all`；15 项 PASS，0 项 FAIL，0 项 SKIP；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`；Markdown H1-H6 各 1、table=1、callout=3、footnote=1；XLSX 导出包经 ExcelJS 回读并再次导入；DOCX 导出包包含正文、编号、脚注和媒体部件并再次回导；无效 DOCX 错误恢复通过。
- 账本：产品 HTML `157279 B / 153.6 KB` → `157437 B / 153.7 KB`（增加行高索引修复）；本次运行夹具 `sample.md=1087 B`、`sample.docx=9127 B`、`sample.xlsx=8771 B`、图片 `68 B`；QA 导出产物和 3 张截图均落盘。
- 有意变更：新增 `tools/fixtures/`、`tools/baseline/`、`tools/output/`；Playwright 浏览器仅用于开发 QA，不进入产品运行时；保留原有单文件 HTML 与 CDN 运行时方案。
- 遗留：M3.3 的本地库缓存/无网启动尚未开发；Excel 条件格式、冻结窗格、数据验证和更深层 OOXML 样式仍按现有转换报告提示降级；DOCX OMML 公式尚未做可逆映射。

## 2026-09-02 · M3.1 第二阶段（Obsidian 语义增强）

- 做了什么：在单文件工作台接入 Frontmatter 识别与原始 YAML 保留、`#标签`/`tags` 汇总、`[[内部链接]]` 与标题锚点、`![[图片]]` 图片嵌入、`![[笔记]]` 占位降级和缺失资源转换报告；Word 预览回写保持原始语法，Excel 仍只消费表格。
- 验证：`npm.cmd run qa:all`；18 项 PASS，0 项 FAIL，0 项 SKIP；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`；Markdown DOM 命中 frontmatter=1、正文标签=2、内部链接=2、嵌入=2（缺失图片=1），语义回写通过；Frontmatter 不泄漏到 Word 正文；DOCX 图片资源转为 `md-embed-image` 的回归通过。
- 账本：产品 HTML `157437 B / 153.7 KB` → `184351 B / 180.0 KB`（增加语义渲染、回写、Word 元数据边界与 QA 断言，+26914 B / +26.3 KB）；本次夹具 `sample.md=1355 B`、`sample.docx=9128 B`、`sample.xlsx=8770 B`、图片 `68 B`；三张截图和 JSON 报告已更新。
- 有意变更：`tools/verify.mjs` 在测试侧用 Node `fetch` 回放固定 jsDelivr 依赖并屏蔽字体网络请求，避免浏览器测试受本机 CDN 代理影响；产品仍保留 CDN 加载与无库降级路径。修复语义 token 与 Markdown 下划线冲突，以及空行触发代码围栏扫描不前进的问题。
- 遗留：Frontmatter 暂只解析简单 key/value、列表和内联数组；笔记嵌入尚未读取其他 Markdown 文件或区块；M3.3 本地库缓存、无网启动和依赖版本锁定仍待开发。

## 2026-09-02 · M3.3（离线依赖增强）

- 做了什么：将单文件产品的 9 个运行库改为版本锁定的启动器；首选 jsDelivr，失败自动切换 unpkg，成功执行并校验全局变量后按版本写入 `localStorage`。缓存优先恢复，缓存损坏/版本不符会重新走容灾链；两个源均不可用时保留 Markdown 编辑区并禁用不可用导出能力。
- 验证：`npm.cmd run qa:all`；22 项 PASS，0 项 FAIL，0 项 SKIP；浏览器 `console.error=0`、`pageerror=0`、HTTP 错误 `0`；首选源 9/9 命中、备源故障转移 9/9、无网缓存恢复 9/9、无缓存降级不白屏且 4 个导出按钮全部禁用；真实 Markdown/DOCX/XLSX 导入—导出—回导仍通过。
- 账本：产品 HTML `184351 B / 180.0 KB` → `191421 B / 186.9 KB`（增加离线依赖启动器、来源状态和 QA 场景，+7070 B / +6.9 KB）；本次夹具 `sample.md=1355 B`、`sample.docx=9126 B`、`sample.xlsx=8770 B`、图片 `68 B`；报告写入 `tools/output/m3.3-report.json`，导出产物写入 `tools/output/playwright/m3.3/`，三张基线截图更新。
- 有意变更：移除 head 中直接阻塞应用的 9 个静态 CDN script 标签，改为单文件内置 loader；缓存键为 `document-workbench-dependency-cache-v1:<库名>`，只接受精确版本源码，顶部状态改为显示“版本已锁定”和“本机缓存可用（可离线）”。QA 回放同时覆盖 jsDelivr、unpkg 和外部网络不可用。
- 遗留：首次打开仍需至少访问一次 jsDelivr 或 unpkg 才能建立运行库缓存；Google Fonts 仍是外部字体资源，断网时回退系统字体；大文件分片/Worker/进度取消和 OMML 公式可逆化继续留给 M3.4 及后续。
