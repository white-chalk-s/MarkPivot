# MarkPivot 样式规范

> 版本：UI Foundation v1.0  
> 日期：2026-09-23  
> 适用范围：首页、规范选择页、自动识别/微调工作台、规范库、导出页  
> 原则：先统一结构与组件，再做视觉精修；禁止单页私有样式无限增长。

---

## 1. 设计目标

MarkPivot 是一个本地运行的文档规范化工具。界面需要传达三件事：

1. **专业**：面向论文、标书、咨询报告、工程文档等正式交付场景。
2. **可靠**：用户需要相信系统不会静默破坏文档。
3. **轻量**：工具应接近“打开即用”，不做重型 SaaS 后台感。

视觉方向统一为：

> **暖白背景 + 橙色品牌强调 + 中性灰结构 + 文档工具型界面**

不追求炫技，不用大面积渐变，不用过度玻璃拟态，不用重阴影。

---

## 2. 设计原则

### 2.1 信息优先

优先展示：

- 当前任务
- 当前文件
- 当前规范
- 应用范围
- 当前预览效果
- 导出结果

装饰元素不得抢占核心操作区。

### 2.2 单一主操作

同一视区只保留一个最明确的主按钮。

例如：

- 首页：`下一步：选择规范`
- 规范页：`使用此规范并开始识别`
- 工作台：`完成调整 · 批量导出`
- 导出页：`导出全部文件`

### 2.3 任务上下文明确

一级页面只保留：

- 首页
- 规范库

进入任务后，再显示任务步骤：

1. 上传文件
2. 选择规范
3. 自动识别 / 微调
4. 批量导出

工作台、导出页都属于某一个 Task，不作为一级导航。

### 2.4 批量与单文件共用一套视觉逻辑

上传 1 个文件就是单文件任务。

上传多个文件或文件夹就是批量任务。

不为批量单独设计另一套流程或另一套页面语言。

---

## 3. Design Tokens

### 3.1 颜色

```css
:root {
  --color-brand: #F97316;
  --color-brand-hover: #EA580C;
  --color-brand-soft: #FFF3E8;

  --color-text: #18181B;
  --color-text-secondary: #71717A;
  --color-text-tertiary: #A1A1AA;

  --color-bg: #F7F6F3;
  --color-surface: #FFFFFF;
  --color-surface-soft: #FAFAF9;

  --color-border: #E4E1DC;
  --color-border-strong: #CBC7C1;

  --color-success: #16A34A;
  --color-warning: #D97706;
  --color-danger: #DC2626;
  --color-info: #2563EB;
}
```

### 3.2 颜色使用规则

品牌橙只用于：

- 主按钮
- 当前步骤
- 当前选中项
- 关键图标
- 品牌点缀

禁止：

- 大面积橙色背景
- 全页面高饱和橙色
- 同时出现多个竞争主色

业务状态色与品牌色分离：

- 成功：绿
- 警告：琥珀
- 错误：红
- 信息：蓝

---

## 4. 字体体系

默认系统字体栈：

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "PingFang SC",
  "Microsoft YaHei",
  sans-serif;
```

不依赖外部在线字体。

推荐字号：

| 用途 | 字号 | 字重 |
|---|---:|---:|
| Hero 标题 | 32px | 700 |
| 页面标题 | 24px | 700 |
| 区块标题 | 18px | 600 |
| 卡片标题 | 16px | 600 |
| 正文 | 14px | 400 |
| 控件文字 | 14px | 400/500 |
| 辅助信息 | 12px | 400 |
| 极弱元信息 | 11px | 400 |

规则：

- 页面内不超过 5 个字号层级。
- 不用纯字体大小表达全部层级，配合字重与留白。
- 正文默认行高 1.6 左右。

---

## 5. 间距体系

基础尺度：

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

规则：

- 组件内部优先使用 8 / 12 / 16。
- 卡片之间优先使用 16 / 24。
- 页面区块之间优先使用 24 / 32。
- 禁止大量出现 13px、17px、19px 等随机值。

---

## 6. 圆角、边框、阴影

```css
--radius-control: 6px;
--radius-card: 10px;
--radius-large: 14px;

--border-default: 1px solid var(--color-border);
--border-strong: 1px solid var(--color-border-strong);
```

阴影只用于区分层级：

```css
--shadow-subtle: 0 1px 2px rgba(0,0,0,.04);
--shadow-card: 0 4px 16px rgba(0,0,0,.05);
```

禁止：

- 大范围强阴影
- 浮夸悬浮卡片
- 同页面多套阴影体系

---

## 7. 控件规范

### 7.1 按钮

统一高度：

- 默认：40px
- 紧凑型：32–36px

按钮类型：

#### Primary

用途：当前页面唯一主操作。

样式：

- 背景：品牌橙
- 文字：白
- Hover：深橙
- 禁止同视区出现多个 Primary

#### Secondary

用途：

- 选择文件
- 上一步
- 恢复模板值
- 上传新模板
- 次级操作

#### Ghost

用于轻量导航或不重要操作。

#### Danger

只用于：

- 删除任务
- 移除规范
- 清空不可逆数据

---

### 7.2 输入框 / Select

统一高度：40px。

状态必须覆盖：

- Default
- Hover
- Focus
- Disabled
- Error

Focus 状态：

- 使用品牌色边框
- 不使用强烈发光效果

---

### 7.3 Tab

两类 Tab：

#### 页面级 Tab

例如：

- 首页
- 规范库

#### 工作台属性 Tab

例如：

- 页面
- 正文
- 标题
- 表格
- 图片

当前项使用：

- 品牌色文字或底边
- 不同时使用过多强调方式

---

## 8. 核心组件

v1 必须统一以下组件：

1. Button
2. Input
3. Select
4. Upload Dropzone
5. Card
6. Choice Card
7. Tabs
8. Stepper
9. File Item
10. Status Badge
11. Form Section
12. Document Preview
13. Scope Switch
14. Empty State

后续新增组件必须优先复用这些基础结构。

---

## 9. Stepper 规范

任务固定为 4 步：

```text
1 上传文件
2 选择规范
3 自动识别 / 微调
4 批量导出
```

状态：

- 未开始
- 当前
- 已完成

规则：

- 当前步骤必须明确。
- 已完成步骤可回看。
- 不增加“自动规范化”“检查”等单独主步骤。
- 自动处理、异常提示、检查信息归入第 3 步工作台内部。

---

## 10. 页面模板

### 10.1 首页 / Landing

职责：

> 只回答“我要处理什么”。

首页只包含：

- 产品标题
- 上传区域
- 上传后的文件列表
- `下一步：选择规范`

首页禁止提前展示：

- 模板选择
- 规范库卡片
- 规范设置
- 工作台配置

---

### 10.2 规范选择页 / Choice Page

职责：

> 回答“这些文件最终应该变成什么样”。

三种入口：

1. 上传模板 / 正确样例
2. 从规范库选择
3. 从空白开始

选择完成后进入工作台。

---

### 10.3 自动识别 / 微调工作台 / Workspace

固定三栏：

```text
┌──────────┬──────────────────────┬──────────────┐
│ 文件列表 │      文档实时预览     │   规范设置    │
└──────────┴──────────────────────┴──────────────┘
```

左栏：

- 文件名
- 当前文件
- 状态

中栏：

- 原文 / 当前效果
- 文档纸面预览
- 缩放
- 页码

右栏：

- 当前文件 / 全部文件
- 当前模板
- 上传新模板
- 页面 / 正文 / 标题 / 表格 / 图片
- 保存到本次任务

---

### 10.4 规范库 / Management

用于：

- 浏览规范
- 新建规范
- 编辑规范
- 复制规范
- 删除规范
- 版本信息

不用于处理具体文件。

---

### 10.5 导出页

职责：

- 展示本任务文件
- 明确导出状态
- 单文件导出 DOCX
- 多文件导出 ZIP
- 可选导出规范 JSON

---

## 11. 工作台布局建议

桌面端：

- 左栏：180–220px
- 中栏：自适应，最小 600px
- 右栏：320–360px

中间文档预览必须是最大视觉区域。

右侧设置不允许无限变宽。

---

## 12. 响应式规则

v1 优先桌面端。

断点：

- ≥ 1280：完整三栏
- 960–1279：缩窄左栏、右栏
- < 960：允许右侧设置下移
- < 768：仅保证基础可用，不追求完整编辑体验

---

## 13. 品牌装饰

允许：

- 暖白底色
- 轻量植物 / 文件 / 纸张插画
- 少量橙色品牌点缀

禁止：

- 让插画占据主要操作面积
- 过度 AI 光效
- 大量发光渐变
- 把产品做成“AI 聊天工具”视觉

MarkPivot 的核心应保持：

> 文档、规范、交付。

---

## 14. UI 开发约束

1. 所有页面必须使用 Token。
2. 不允许单页复制一套按钮样式。
3. 不允许在业务组件中硬编码品牌色。
4. 修改基础组件时优先统一升级。
5. 不因某一页面临时需求破坏全局组件契约。
6. 高保真页面先覆盖首页、规范选择页、工作台。
7. 视觉精修在功能稳定后进行。

---

## 15. 当前视觉基线

当前基线：

> **暖白 + 橙色 + 专业文档工具**

该方向作为 UI Foundation v1 的默认视觉，不再继续大规模探索完全不同的风格。

后续主要调整：

- 留白
- 字号
- 组件密度
- 插画
- 动效
- 品牌细节

而不是重做整体视觉语言。
